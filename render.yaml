services:
  - type: web
    name: worldcup2026-simulator
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn src.web_app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120
    envVars:
      - key: ENABLE_SCHEDULE_NETWORK
        value: "1"
